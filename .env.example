VITE_API_BASE_URL=http://localhost:8000
VITE_SSE_URL=http://localhost:8000/api/sse/lecturas

# Acceso con Google (RF-17, método alternativo). Opcional.
# Sin este valor el botón de Google no se muestra y NO se carga código de
# Google: el build queda idéntico al de una instalación sin OAuth.
# Debe coincidir con GOOGLE_CLIENT_ID del backend.
VITE_GOOGLE_CLIENT_ID=
